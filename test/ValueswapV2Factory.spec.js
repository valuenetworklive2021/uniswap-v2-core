const waffle = require('ethereum-waffle');
const chai = require('chai');
const { expect } = chai;
const V2Factory = artifacts.require('ValueswapV2Factory');



chai.use(waffle.solidity);

contract('ValueswapV2Factory::', (accounts) => {
  it('...should be ok.', async () => {
  });
});
